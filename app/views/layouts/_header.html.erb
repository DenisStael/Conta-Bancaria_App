<nav class="navbar navbar-inverse navbar-static-top">
    <div class="container-fluid">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#expanded-menu"
                aria-expanded=" false">
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <%= link_to 'Conta Bancária', root_path, class: 'navbar-brand' %>
        </div>

        <div class="collapse navbar-collapse" id="expanded-menu">
            <ul class="nav navbar-nav navbar-right">
                <% if !user_signed_in? %>
                <li>
                    <%= link_to 'Acessar Conta', login_path %>
                </li>
                <% else %>
                <li class="dropdown">
                    <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">
                        Bem vindo, <%= current_user.name %> <span class="caret"></span>
                    </a>
                    <ul class="dropdown-menu">
                        <li>
                            <%= link_to "Conta", account_path(current_user) %>
                        </li>
                        <li>
                            <%= link_to "Nova operação", new_account_operation_path %>
                        </li>
                        <li>
                            <%= link_to "Extrato", account_operations_path %>
                        </li>
                        <li class="divider" role="separator"></li>
                        <li>
                            <%= link_to 'Sair', logout_path, method: :delete %>
                        </li>
                    </ul>
                </li>
                <% end %>
            </ul>
            <ul class="nav navbar-nav navbar-right">
                <% if !user_signed_in? %>
                <li>
                    <%= link_to 'Criar Conta', new_account_path %>
                </li>
                <% end %>
            </ul>
        </div>
    </div>
</nav>